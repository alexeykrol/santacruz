---
description: Создать git commit для литературного проекта
---

Создай git commit следуя лучшим практикам для литературного проекта.

## Процесс:

### 1. Анализ изменений
Выполни параллельно:
- `git status` - посмотри все измененные файлы
- `git diff` - посмотри unstaged изменения
- `git diff --staged` - посмотри staged изменения
- `git log --oneline -n 5` - посмотри последние коммиты для стиля

### 2. Что можно коммитить

✅ МОЖНО коммитить:
- Markdown файлы (`.md`)
- Мета-файлы (`BACKLOG.md`, `PROJECT_SNAPSHOT.md`, и т.д.)
- Текстовые файлы проекта (сцены, эпизоды, персонажи)
- Обновления документации

❌ НЕ коммитить:
- Временные файлы (`.DS_Store`, `*.tmp`, `*.log`)
- Личные заметки автора (если не часть проекта)
- Файлы редакторов (`.idea/`, `.vscode/` если не в .gitignore)

### 3. Добавь нужные файлы

Добавляй файлы **по отдельности** или группами:
```bash
git add meta/BACKLOG.md meta/PROJECT_SNAPSHOT.md
```

Или по паттерну:
```bash
git add 3_stories/**/*.md
```

### 4. Составь commit message

**Формат:**
```
<type>: <краткое описание> (до 50 символов)

<детальное описание - ПОЧЕМУ, а не ЧТО>
- Причина изменения
- Какую проблему решает
- Важные детали

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

**Types для литературного проекта:**
- `docs`: обновление worldbuilding, персонажей, концепций
- `feat`: завершение фазы, новый эпизод/глава
- `edit`: исправления текста, редактура
- `meta`: обновление мета-файлов (BACKLOG, SNAPSHOT)
- `structure`: изменения в структуре проекта

**Примеры хороших сообщений:**

```
docs: answer Q2.1 - identify attacker in Santa Cruz incident

Определена атакующая сторона как [краткое описание].
- Добавлен файл attackers.md с обоснованием
- Обновлена онтология с новыми концепциями
- Проверена консистентность с существующими фактами

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

```
feat: complete Phase 1 - worldbuilding

Завершена проработка критичных вопросов Q1-Q4.
- Отвечено на 16 критичных вопросов из universe_questions.md
- Обновлены файлы в 1_universe/ с новыми концепциями
- Проверена консистентность всей онтологии

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

```
meta: update progress after worldbuilding session

Обновлены мета-файлы после рабочей сессии.
- BACKLOG.md: отмечены выполненные задачи
- PROJECT_SNAPSHOT.md: обновлен прогресс (5% → 15%)
- Добавлены новые блокеры

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
```

### 5. Создай коммит

Используй HEREDOC для правильного форматирования:

```bash
git commit -m "$(cat <<'EOF'
<type>: <краткое описание>

<детальное описание>

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"
```

### 6. Проверь результат
```bash
git log -1 --stat
```

## 📋 Checklist
- [ ] `git status` - проверил изменения
- [ ] `git diff` - понимаю что изменил
- [ ] Проверил стиль последних коммитов
- [ ] Добавил ТОЛЬКО нужные файлы (не `git add .` без проверки)
- [ ] Создал осмысленное сообщение (why, not what)
- [ ] Добавил Co-Authored-By: Claude
- [ ] `git log -1` - проверил результат

## 🚫 НЕ делай:
- ❌ `git add .` без проверки что добавляешь
- ❌ Расплывчатые сообщения ("fix", "update", "changes")
- ❌ Коммит временных файлов (.DS_Store, *.tmp)
- ❌ `--no-verify` (пропуск hooks) без явного указания пользователя

**Если пользователь не указал что коммитить - спроси его!**
